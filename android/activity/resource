从代码中访问资源：
[<package_name>.]R.<resource_type>.<resource_name>

resource_type有：
R.drawable ，R.anim ,R.color，R.layout，R.menu ，R.string，R.array，R.style....

获取资源：
1,获取Resource,Context.getResources()
2,通过resource获取资源，resource.getText/...

获取xml中定义的View(button,label)：
findViewById(R.id.xxx)

从xml中访问资源：
@[<package_name>:]<resource_type>/<resource_name>
